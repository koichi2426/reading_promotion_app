import 'package:flutter/material.dart';

class ReadComponent extends StatelessWidget {
  const ReadComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('ReadComponent');
  }
}