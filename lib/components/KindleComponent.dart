import 'package:flutter/material.dart';

class KindleComponent extends StatelessWidget {
  const KindleComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('KindleComponent');
  }
}